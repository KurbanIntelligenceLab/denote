[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "denote"
version = "1.0.0"
description = "Code and data for DENOTE: Corruption Scores Bound Chain-of-Thought Faithfulness From Above (IEEE TDSC)"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }

[tool.setuptools.packages.find]
where = ["src"]
